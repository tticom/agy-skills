interface:
  display_name: "Code Review"
  short_description: "Run a safe exact-head basic PR review"
  default_prompt: "Use $code-review to perform a basic exact-head review, leave inline findings where needed, and publish the mandatory PR summary without modifying the branch."
